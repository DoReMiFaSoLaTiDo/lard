<div>
  <h1>Players</h1>
  <p>Soon this will show a list of all registered players, based on information from the server</p>
</div>

<div>
  <table>
    <thead>
      <td>Player Name</td>
      <td>Position</td>
      <td>Club</td>
    </thead>
    <tbody>
      <tr ng-repeat="player in players">
        <td>{{player.name}}</td>
        <td>{{player.position}}</td>
        <td>{{player.club}}</td>
      </tr>
    </tbody>
  </table>
</div>
